t = int(input())
for _ in range(t):
    n, k = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    rem = a[k:n-k]
    res = sum(rem) / len(rem)
    print(f"{res:.6f}")
        
