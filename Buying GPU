import math
def solve():
    t = int(input())
    for _ in range(t):
        x, y, z = map(int,input().split())
        if z <= y:
            print(-1)
        else:
            res = math.ceil(x / (z - y))
            print(res)
if __name__ == "__main__":
    solve()
