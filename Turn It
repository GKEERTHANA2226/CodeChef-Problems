T = int(input())

for _ in range(T):
    U, V, A, S = map(int, input().split())

    speed_squared = U * U - 2 * A * S

    if speed_squared <= V * V:
        print("Yes")
    else:
        print("No")
