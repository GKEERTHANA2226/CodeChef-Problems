t = int(input())
for _ in range(t):
    n = int(input())
    s = list(map(int, input().split()))
    s.sort()
    res = float('inf')
    for i in range(n-1):
        res = min(res, s[i+1] - s[i])
    print(res)
        
